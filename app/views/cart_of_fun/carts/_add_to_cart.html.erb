  <%= form_for book, url: cart_of_fun.add_item_cart_path, method: :post do |f| %>
    <%= number_to_currency book.price %>
    <%= f.number_field :quantity, value: 1, min: 1, max: 9 %>
    <%= f.hidden_field :book_id, value: book.id %>
    <%= f.submit 'Add to cart'%>
  <% end %>
